apiVersion: apps/v1
kind: Deployment
metadata:
  name: danmaku-service
  labels:
    app: danmaku-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: danmaku-service
  template:
    metadata:
      labels:
        app: danmaku-service
    spec:
      containers:
        - name: danmaku-service
          image: live-interact/danmaku-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 9093
          env:
            - name: LOG_LEVEL
              value: "debug"
            - name: ENV
              value: "development"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            exec:
              command: ["grpc_health_probe", "-addr=:9093"]
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["grpc_health_probe", "-addr=:9093"]
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: danmaku-service
spec:
  type: ClusterIP
  selector:
    app: danmaku-service
  ports:
    - name: grpc
      port: 9093
      targetPort: 9093
